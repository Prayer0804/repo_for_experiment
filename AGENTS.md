你是一个全自动执行型高级工程代理。请从现在开始，围绕“基于交通大数据的处理与分析”项目，独立完成从项目搭建、数据获取、数据处理、分析建模、实时模拟、可视化、测试、文档、运行说明到最终交付的全部任务。

核心要求：
1. 不要只给方案，必须实际修改/创建代码、配置、脚本、文档和测试。
2. 不允许偷工减料，不允许用空壳文件、伪代码、TODO、占位实现冒充完成。
3. 采用“计划 → 执行 → 自检 → 修复 → 再自检”的循环，直到所有验收标准通过。
4. 遇到错误要自动诊断并修复，不要因为第一次失败就停止。
5. 只有在确实缺少外部权限、账号、网络或数据访问被阻断时，才向我明确说明阻塞点、已完成内容、未完成内容和可替代方案。
6. 每完成一个阶段都要更新 README 或进度文档，确保后续人员可复现。
7. 最终输出必须包含：项目结构说明、运行方式、数据处理流程、分析指标、模型说明、可视化说明、测试结果、已知限制和后续改进建议。

项目背景：
本项目主题是“基于交通大数据的处理与分析”。目标是使用纽约市 TLC 出租车数据集完成一个四人全栈工具链式大数据项目。数据集规模大、结构化程度高、字段语义清晰，适合 Hive/Spark 数仓、机器学习预测、实时模拟和大屏展示。

请围绕以下技术路线完成项目：

一、总体架构
实现或模拟以下数据流：
数据源：纽约市 TLC Taxi Trip Record Data
存储与数仓：HDFS / 本地模拟 HDFS / Hive / Parquet
数据分层：
- Bronze 层：原始数据落地，保留原始字段，完成格式统一
- Silver 层：清洗、过滤异常值、字段标准化、时间和空间特征派生
- Gold 层：面向分析和可视化的宽表、聚合表、指标表
计算引擎：
- Spark Core / Spark SQL：离线批处理和指标计算
- Spark MLlib：需求预测或行程时长/费用预测
- Kafka + Flink：实时流处理模拟，可在本地环境中用脚本模拟消息流
应用展示：
- ECharts 可视化大屏，展示交通需求、热点区域、时段趋势、预测结果等

二、必须完成的功能模块
请在仓库中完成以下模块，不能遗漏：

1. 项目初始化
- 设计清晰的目录结构
- 提供 requirements.txt / pyproject.toml / environment.yml 或等价依赖文件
- 如果适合，提供 docker-compose.yml，用于本地启动相关组件
- 提供 .gitignore
- 提供 config 配置文件，避免硬编码路径

2. 数据获取与样例数据
- 编写数据下载脚本，支持从 NYC TLC 官方数据地址下载指定月份数据
- 如果网络不可用，生成结构一致的 mock TLC 样例数据
- 样例数据必须包含典型字段，例如 pickup/dropoff 时间、上下车经纬度或区域 ID、乘客数、距离、费用、支付方式等
- 数据脚本必须可重复执行，不能每次产生不可控结果；如有随机数，设置 seed

3. Bronze 层
- 读取原始 CSV/Parquet 数据
- 统一字段命名
- 保留原始记录
- 输出 Bronze Parquet 文件
- 记录数据行数、字段数、缺失值统计

4. Silver 层
- 完成数据清洗：
  - 删除或标记空时间、负距离、负费用、异常乘客数
  - 过滤明显异常的 trip_distance、fare_amount、duration
  - 解析 pickup_datetime 和 dropoff_datetime
  - 计算 trip_duration_min
  - 提取 hour、weekday、date、month 等时间特征
  - 构建可用于分析的区域/OD 特征
- 输出 Silver Parquet 文件
- 生成清洗质量报告，包括清洗前后行数、异常记录数量、主要字段统计

5. Gold 层
至少生成以下分析表：
- 按小时统计订单量、平均行程距离、平均费用、平均时长
- 按日期统计订单量和收入趋势
- 按 pickup 区域统计热点区域
- 按 OD 组合统计高频出行路线
- 按支付方式统计订单和费用
- 用于预测任务的特征表

6. 离线分析
请实现若干分析脚本，输出 CSV/Parquet/JSON 结果：
- 城市交通需求时段规律分析
- 早晚高峰分析
- 区域热点分析
- 出行距离与费用关系分析
- 订单量与时间特征关系分析
- OD 高频路线分析

7. 机器学习预测
使用 Spark MLlib 或可本地运行的等价实现完成至少一个预测任务：
优先任务：按小时或区域预测出租车需求量
备选任务：预测行程费用或行程时长
要求：
- 完成特征工程
- 划分训练集/测试集
- 至少实现一个 baseline 模型和一个改进模型
- 输出 RMSE、MAE、R2 或合适指标
- 保存模型或模型参数
- 生成模型评估报告
- 不允许只写模型代码但不跑通流程

8. 实时流处理模拟
实现 Kafka + Flink 或本地可运行的等价模拟：
- 编写生产者脚本，逐条发送或模拟出租车行程事件
- 编写消费者/流处理脚本，统计滑动窗口内订单量、平均费用、热点区域
- 输出实时统计结果到 JSON/CSV 或 WebSocket 接口
- 如果无法启动 Kafka/Flink，必须提供 fallback 本地流模拟版本，并在文档中说明

9. 可视化大屏
实现一个可运行的前端页面，优先使用 ECharts：
展示至少以下图表：
- 每小时订单量折线图
- 每日订单量/收入趋势图
- 热点区域柱状图或地图替代图
- 高频 OD 路线排行
- 支付方式占比图
- 预测值 vs 实际值对比图
要求：
- 前端能读取 Gold 层导出的 JSON/CSV 数据
- 页面布局清晰，适合作为项目展示大屏
- 提供启动方式
- 不允许只放静态截图，必须有可运行代码

10. 报告与文档
生成以下文档：
- README.md：完整运行说明
- docs/architecture.md：系统架构说明
- docs/data_pipeline.md：Bronze/Silver/Gold 数据流说明
- docs/data_quality_report.md：数据质量与清洗报告
- docs/analysis_report.md：分析结果说明
- docs/model_report.md：模型训练与评估说明
- docs/realtime_report.md：实时流处理说明
- docs/team_work_split.md：四人团队分工，覆盖数据工程、数据分析、算法预测、可视化开发
- docs/final_delivery_checklist.md：最终交付清单

11. 测试与质量保障
- 为关键数据清洗逻辑编写单元测试
- 为特征工程函数编写测试
- 为数据输出路径和 schema 检查编写测试
- 提供一键测试命令
- 修复所有能修复的测试失败
- 运行 lint 或基础代码检查；如果没有 lint 工具，则至少保证 Python 文件可被编译

三、推荐目录结构
请根据实际情况创建或优化为类似结构：

traffic-bigdata-analysis/
  README.md
  requirements.txt
  docker-compose.yml
  config/
    config.yaml
  data/
    raw/
    bronze/
    silver/
    gold/
    sample/
  scripts/
    download_tlc_data.py
    generate_mock_data.py
    run_all.py
  src/
    common/
    data_ingestion/
    bronze/
    silver/
    gold/
    analysis/
    ml/
    streaming/
    visualization_api/
  frontend/
    package.json
    src/
    public/
  notebooks/
  tests/
  docs/
  outputs/
    reports/
    charts/
    metrics/

四、自动执行策略
请严格按以下循环执行：

Step 1：检查当前仓库状态，识别已有文件、缺失模块和可复用内容。
Step 2：制定详细任务清单，写入 docs/final_delivery_checklist.md。
Step 3：逐项实现功能，不要跳过任何核心模块。
Step 4：每完成一个模块，运行最小可行验证。
Step 5：发现错误立即修复。
Step 6：运行完整流水线：
        数据准备 → Bronze → Silver → Gold → 分析 → 模型 → 实时模拟 → 可视化数据导出 → 测试
Step 7：检查所有交付物是否存在且内容非空。
Step 8：如果任何验收项失败，回到对应模块修复。
Step 9：直到所有验收项通过后，输出最终总结。

五、验收标准
只有满足以下条件，才能认为任务完成：

- 项目可以通过一条命令或清晰步骤跑通主流程
- 即使没有真实 TLC 大数据，也能使用 mock/sample 数据完整演示
- Bronze/Silver/Gold 三层数据均有实际输出
- 至少 5 类分析结果有实际输出文件
- 至少 1 个机器学习预测任务完整跑通并输出评估指标
- 实时流处理或本地流模拟可以运行
- ECharts 或等价前端大屏可以展示结果
- README 和 docs 文档完整
- 测试可以运行，关键测试通过
- 没有未完成的 TODO、空实现、伪代码冒充实现
- 最终总结中列出实际生成的文件和运行过的命令

六、输出格式
你在执行过程中请持续给出简洁进度，但重点是实际完成文件修改。
最终回复请包含：
1. 已完成模块列表
2. 关键文件路径
3. 如何一键运行
4. 如何查看可视化大屏
5. 测试结果
6. 未能完成的内容及原因；如果全部完成，请明确写“全部验收项已完成”
7. 后续可扩展建议

现在开始执行。不要停留在计划阶段。请立刻检查仓库、创建缺失文件、实现代码、运行验证并循环修复，直到所有验收项完成。